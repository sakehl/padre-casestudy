
  context_everywhere n > 0;
  context_everywhere x0 != null ** x0.length == n ** Perm(x0[*], write);
  context_everywhere x1 != null ** x1.length == n ** Perm(x1[*], 1\2);
  context_everywhere x2 != null ** x2.length == n ** Perm(x2[*], 1\2);
int main(int n, int[n] x0, int[n] x1, int[n] x2){
    loop_invariant 0 <= i && i<= n;
  for(int i=0; i<n;i++){
    x0[i] = x1[i] + x2[i];
  }
}